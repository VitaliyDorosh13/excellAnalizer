# Frontend Module

Planned responsibilities:

- desktop user interface built with React and TypeScript
- workbook navigation and preview
- findings dashboard
- profile editor for mappings and validation rules
- plugin management UI

Included in this scaffold:

- Vite-based React app entrypoint
- operator dashboard wired to backend API flows
- Tauri configuration and Rust shell entrypoint
- API client for preview, normalize, validate, and export

Suggested next implementation steps:

1. Add table-focused preview and findings drill-down views
2. Create saved profile management for validation and export mappings
3. Connect plugin management actions beyond registry read
4. Add file picker integration through Tauri
