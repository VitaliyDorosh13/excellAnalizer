# Plugins

This directory is reserved for optional extension packages.

Planned plugin categories:

- file format parsers
- connector adapters
- validation rule packs
- transformation packs

Initial plugin targets:

- PDF parser plugin
- sample REST connector plugin
- sample custom rule plugin
